import 'package:flutter/cupertino.dart';

class OnErrorImage extends StatelessWidget {
  const OnErrorImage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
